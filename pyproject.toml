[dependency-groups]
dev = ["actionlint-py>=1.7.12.24"]
backend = ["chainlit-litestar"]

[tool.mypy]
python_version = "3.14"

[[tool.mypy.overrides]]
module = [
    "boto3.dynamodb.types",
    "botbuilder.*",
    "filetype",
    "langflow",
    "lazify",
    "plotly",
    "socketio.*",
    "syncer",
    # No release installable on 3.14: the ones allowing pydantic >=2.12
    # pull a pre-release azure-ai-agents, so it is absent from the tests extra.
    "semantic_kernel.*",
    "azure.storage.filedatalake",
    "google-cloud-storage",
    "azure.storage.blob",
    "azure.storage.blob.aio",
    "google.auth",
    "google.oauth2",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py314"

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "LOG",
    "UP",
    "T10",
    "ISC",
    "ICN",
    "LOG",
    "G",
    "PIE",
    "PT",
    "Q",
    "RSE",
    "FURB",
    "RUF",
]
ignore = [
    "E712",
    "E501",
    "UP006",
    "UP007",
    "UP035",
    "UP045",
    "PIE790",
    "RUF005",
    "RUF006",
    "RUF012",
    "ISC001",
    # Unquoting annotations breaks runtime introspection: names imported only
    # under TYPE_CHECKING resolve fine for mypy but raise NameError when
    # something evaluates the annotation (Mock(spec=...), inspect.signature).
    "UP037",
    # Restyling public types buys nothing and changes what consumers see:
    # UP042 would turn Theme into a StrEnum, changing str(Theme.light) from
    # "Theme.light" to "light"; UP046/UP047 would move public generics to PEP
    # 695 parameter lists.
    "UP042",
    "UP046",
    "UP047",
]

[tool.ruff.lint.isort]
combine-as-imports = true
known-first-party = ["chainlit"]

[tool.uv]
default-groups = ["backend"]
package = false

[tool.uv.sources]
chainlit-litestar = { workspace = true }

[tool.uv.workspace]
members = ["backend"]
